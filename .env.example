# Neon Database Configuration (via Netlify DB)
# This is automatically provided by Netlify when you create a Netlify DB
# Format: postgresql://[user]:[password]@[neon_hostname]/[dbname]?sslmode=require
DATABASE_URL=postgresql://user:password@ep-example.us-east-2.aws.neon.tech/neondb

# Optional: Enable authentication
ENABLE_AUTH=false
